Migrated Spring Core project with Spring boot .
1. Logger configured .
2. There are 2 profiles (“release” and “debug”).
   when you can the property "spring.profiles.active" in application.properties it with select configurated accordingly .
   Logging pattern will be changed based on the selected profile .